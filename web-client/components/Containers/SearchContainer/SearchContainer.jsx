import React from 'react';

const SearchContainer = ({children}) => {
    return (
        <div>
            <h1>Аудитории</h1>
            {children}
        </div>
    );
};

export default SearchContainer;